spring:
  application:
    name: UserManager

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka

logging:
  pattern:
    console: "%C{1.} [%-5level] %d{HH:mm:ss} - %msg%n"
